# Title goes here

<!--
  How to publish a note:
    1. Copy this file to notes/YYYY-MM-DD-slug.md (delete this comment block).
    2. Write it. The first line above becomes the page <h1> -- it's stripped
       out when rendered here, so leave it as a plain "# Title" line.
    3. Add one entry to notes/manifest.js with the same slug:
         { slug: "YYYY-MM-DD-slug", title: "...", date: "YYYY-MM-DD", summary: "..." }
    4. git add, commit, push. No build step -- it's live as soon as it's pushed.

  This file itself is never listed in manifest.js, so it won't show up
  anywhere on the site. It's just a starting point to copy.
-->

Regular paragraphs, same as any Markdown file.

## A section heading

- bullet
- points
- work

Inline math: $z \sim N(0,1)$.

Display math:

$$ p = \frac{1 + \#\{\, T_{\text{perm}} \ge T_{\text{obs}} \,\}}{1 + \text{replications}} $$

A [link](/methods.html), and `inline code`.

> A blockquote, if needed.
